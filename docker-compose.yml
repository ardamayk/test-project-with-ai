services:
  server:
    build: .
    network_mode: host
    environment:
      SERVER_ADDR: "127.0.0.1:8090"
      DATABASE_PATH: /data/app.db
    volumes:
      - app-data:/data
      - ${MUSIC_PATH:-./music}:/music:ro

volumes:
  app-data:
