# Navidrome Replacement

Self-hosted music server with a modular widget layout and plugin-ready architecture.

- **API reference:** `/api/docs` (Swagger UI)
- **Web UI:** served by the Go server in production
