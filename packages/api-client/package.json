{
  "name": "@repo/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "format": "pnpm --dir ../.. exec biome format --write packages/api-client",
    "format:check": "pnpm --dir ../.. exec biome format packages/api-client",
    "lint": "pnpm --dir ../.. exec biome lint packages/api-client",
    "check": "pnpm --dir ../.. exec biome check packages/api-client",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "generate": "echo 'Generated via @repo/contracts'"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "typescript": "^6.0.2",
    "vitest": "^4.1.5"
  }
}
