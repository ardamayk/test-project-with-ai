openapi: 3.0.3
info:
  title: Navidrome Replacement API
  version: 0.1.0
  description: Self-hosted music server API
servers:
  - url: http://localhost:8090
    description: Local development
tags:
  - name: system
  - name: user
  - name: preferences
  - name: library
  - name: imports
  - name: playback
  - name: playlists
  - name: radio
paths:
  /api/v1/health:
    get:
      tags: [system]
      operationId: getHealth
      summary: Health check
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /api/v1/me:
    get:
      tags: [user]
      operationId: getMe
      summary: Current user profile (stub)
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/preferences:
    get:
      tags: [preferences]
      operationId: getPreferences
      summary: Get user preferences including layout
      security:
        - bearerAuth: []
      responses:
        "200":
          description: User preferences
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserPreferences"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      tags: [preferences]
      operationId: patchPreferences
      summary: Update user preferences
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserPreferencesPatch"
      responses:
        "200":
          description: Updated preferences
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserPreferences"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/library/artists:
    get:
      tags: [library]
      operationId: listArtists
      summary: List artists
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: q
          in: query
          schema:
            type: string
          description: Search by name
      responses:
        "200":
          description: Artist list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtistList"
  /api/v1/library/albums:
    get:
      tags: [library]
      operationId: listAlbums
      summary: List albums
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: artistId
          in: query
          schema:
            type: string
            format: uuid
        - name: q
          in: query
          schema:
            type: string
          description: Search by title
      responses:
        "200":
          description: Album list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AlbumList"
  /api/v1/library/albums/{albumId}:
    get:
      tags: [library]
      operationId: getAlbum
      summary: Get album with tracks
      parameters:
        - $ref: "#/components/parameters/albumId"
      responses:
        "200":
          description: Album detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AlbumDetail"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [library]
      operationId: deleteAlbum
      summary: Permanently delete every Managed Track of an Album after reviewing the album deletion preview
      description: >-
        Runs one Permanent Track Deletion per Track in album order. Each Track
        commits on its own: a failure stops the run, Tracks already deleted stay
        deleted, and the result lists both outcomes. The Album and its artwork
        disappear with their last Track.
      parameters:
        - $ref: "#/components/parameters/albumId"
        - name: X-Permanent-Delete
          in: header
          required: true
          description: Explicit application confirmation required for irreversible deletion
          schema:
            type: string
            enum: ["1"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TrackDeletionConfirmation"
      responses:
        "200":
          description: Per-Track outcome of the album deletion
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AlbumDeletionResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/library/albums/{albumId}/deletion:
    get:
      tags: [library]
      operationId: previewAlbumDeletion
      summary: Review the irreversible deletion of every Managed Track of an Album and all affected references
      parameters:
        - $ref: "#/components/parameters/albumId"
      responses:
        "200":
          description: Current album deletion preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AlbumDeletionPreview"
        "409":
          $ref: "#/components/responses/Conflict"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/library/albums/{albumId}/cover:
    get:
      tags: [library]
      operationId: getAlbumCover
      summary: Get album cover image
      parameters:
        - $ref: "#/components/parameters/albumId"
      responses:
        "200":
          description: Cover image
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/library/tracks:
    get:
      tags: [library]
      operationId: listTracks
      summary: List or search tracks
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: q
          in: query
          schema:
            type: string
          description: Search by title or artist
      responses:
        "200":
          description: Track list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackList"
  /api/v1/library/tracks/{trackId}:
    get:
      tags: [library]
      operationId: getTrack
      summary: Get track metadata
      parameters:
        - $ref: "#/components/parameters/trackId"
      responses:
        "200":
          description: Track metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Track"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [library]
      operationId: deleteTrack
      summary: Permanently delete a Managed Track after reviewing its deletion preview
      parameters:
        - $ref: "#/components/parameters/trackId"
        - name: X-Permanent-Delete
          in: header
          required: true
          description: Explicit application confirmation required for irreversible deletion
          schema:
            type: string
            enum: ["1"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TrackDeletionConfirmation"
      responses:
        "200":
          description: Managed Track and its exact managed audio file permanently deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/library/tracks/{trackId}/deletion:
    get:
      tags: [library]
      operationId: previewTrackDeletion
      summary: Review irreversible Managed Track deletion and all affected references
      parameters:
        - $ref: "#/components/parameters/trackId"
      responses:
        "200":
          description: Current Permanent Track Deletion preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackDeletionPreview"
        "409":
          $ref: "#/components/responses/Conflict"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/library/tracks/{trackId}/replacement:
    post:
      tags: [library]
      operationId: createTrackReplacement
      summary: Start an explicit Track Replacement for one Managed Track
      description: >-
        Creates a Managed Import Job bound to the Track. Upload the replacement file
        with uploadManagedImportFile; the returned Import Preview carries a
        replacement section listing every difference and destructive consequence.
        Confirm with confirmTrackReplacement, never with confirmManagedImport.
      parameters:
        - $ref: "#/components/parameters/trackId"
      responses:
        "201":
          description: Track Replacement job created and awaiting the replacement upload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedImportJob"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /api/v1/import-batches:
    post:
      tags: [imports]
      operationId: createManagedImportBatch
      summary: Create one multi-file Managed Import Batch
      responses:
        "201":
          description: Managed Import Batch created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedImportBatch"
  /api/v1/import-history:
    get:
      tags: [imports]
      operationId: listImportHistory
      summary: List up to 20 most recent terminal Managed Import results
      responses:
        "200":
          description: Recent terminal Import History without staged audio or raw metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedImportHistoryList"
  /api/v1/import-batches/{batchId}:
    get:
      tags: [imports]
      operationId: getManagedImportBatch
      summary: Get per-file preview, progress, selection, and outcomes
      parameters:
        - $ref: "#/components/parameters/batchId"
      responses:
        "200":
          description: Current Managed Import Batch
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedImportBatch"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [imports]
      operationId: cancelManagedImportBatch
      summary: Cancel an uncommitted Managed Import Batch and remove its staging
      parameters:
        - $ref: "#/components/parameters/batchId"
      responses:
        "204":
          description: Managed Import Batch canceled and staging removed
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /api/v1/import-batches/{batchId}/confirm:
    post:
      tags: [imports]
      operationId: confirmManagedImportBatch
      summary: Commit selected accepted files independently and return all outcomes
      parameters:
        - $ref: "#/components/parameters/batchId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ManagedImportBatchConfirmation"
      responses:
        "200":
          description: Terminal per-file Import Batch report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedImportBatch"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /api/v1/imports:
    post:
      tags: [imports]
      operationId: createManagedImportJob
      summary: Create one server-owned Managed Import Job
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ManagedImportJobCreate"
      responses:
        "201":
          description: Managed Import Job created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedImportJob"
  /api/v1/imports/{importId}:
    get:
      tags: [imports]
      operationId: getManagedImportJob
      summary: Observe Managed Import validation progress and terminal result
      parameters:
        - $ref: "#/components/parameters/importId"
      responses:
        "200":
          description: Current Managed Import Job result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedImportJob"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [imports]
      operationId: cancelManagedImport
      summary: Cancel an uncommitted Managed Import Job and remove its staging
      parameters:
        - $ref: "#/components/parameters/importId"
      responses:
        "204":
          description: Managed Import Job canceled and staging removed
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /api/v1/imports/{importId}/file:
    put:
      tags: [imports]
      operationId: uploadManagedImportFile
      summary: Stream one audio file into a Managed Import Job and detect its actual format
      parameters:
        - $ref: "#/components/parameters/importId"
        - name: X-Import-Filename
          in: header
          required: true
          schema:
            type: string
          description: Display-only client filename; never used as a storage path.
        - name: X-Import-Filename-Encoding
          in: header
          required: false
          schema:
            type: string
            enum: [url]
          description: >-
            Set to url when X-Import-Filename is percent-encoded so non-ASCII
            filenames survive HTTP header transport. Absent means the header is
            sent verbatim.
      description: >-
        The request media type is advisory only. The Music Server detects the
        Source Audio Format from the uploaded bytes and never trusts the
        declared Content-Type or the client filename extension.
      requestBody:
        required: true
        content:
          application/octet-stream:
            schema:
              type: string
              format: binary
          audio/flac:
            schema:
              type: string
              format: binary
          audio/wav:
            schema:
              type: string
              format: binary
          audio/mpeg:
            schema:
              type: string
              format: binary
          audio/mp4:
            schema:
              type: string
              format: binary
          audio/ogg:
            schema:
              type: string
              format: binary
          audio/opus:
            schema:
              type: string
              format: binary
      responses:
        "200":
          description: Import Preview ready for confirmation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedImportPreview"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          description: Upload stream was interrupted and this Import Job remains retryable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          $ref: "#/components/responses/Conflict"
        "413":
          description: File or batch exceeds the configured Managed Import byte limit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: File fails the Strict Import Profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "507":
          $ref: "#/components/responses/InsufficientStorage"
  /api/v1/imports/{importId}/confirm:
    post:
      tags: [imports]
      operationId: confirmManagedImport
      summary: Confirm an Import Preview and commit its Managed Track
      parameters:
        - $ref: "#/components/parameters/importId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ManagedImportConfirmation"
      responses:
        "200":
          description: Managed Track committed, or the idempotent prior result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedImportResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "507":
          $ref: "#/components/responses/InsufficientStorage"
  /api/v1/imports/{importId}/replacement:
    post:
      tags: [imports]
      operationId: confirmTrackReplacement
      summary: Confirm a reviewed Track Replacement and delete the previous managed file
      description: >-
        Keeps the Track ID and every Playlist and Queue reference, makes the validated
        replacement metadata authoritative, cleans emptied Albums and Artists, and
        deletes the previous managed file only after the replacement was placed,
        hashed, committed, and read back from its Canonical Library Path.
      parameters:
        - $ref: "#/components/parameters/importId"
        - name: X-Track-Replacement
          in: header
          required: true
          description: Explicit application confirmation required for the irreversible old-file deletion
          schema:
            type: string
            enum: ["1"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TrackReplacementConfirmation"
      responses:
        "200":
          description: Track Replacement committed, or the idempotent prior result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackReplacementResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          description: Replacement file no longer passes the Strict Import Profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "507":
          $ref: "#/components/responses/InsufficientStorage"
  /api/v1/playback/queue:
    get:
      tags: [playback]
      operationId: getPlaybackQueue
      summary: Get playback queue
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Current queue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Queue"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      tags: [playback]
      operationId: replacePlaybackQueue
      summary: Replace playback queue
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueueReplace"
      responses:
        "200":
          description: Updated queue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Queue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Queue revision conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueConflictResponse"
    patch:
      tags: [playback]
      operationId: reorderPlaybackQueue
      summary: Reorder playback queue items
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueueReorder"
      responses:
        "200":
          description: Updated queue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Queue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Queue revision conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueConflictResponse"
  /api/v1/playback/queue/items:
    post:
      tags: [playback]
      operationId: appendPlaybackQueueItem
      summary: Append track to queue
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueueItemAppend"
      responses:
        "200":
          description: Updated queue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Queue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Queue revision conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueConflictResponse"
  /api/v1/playback/queue/items/{itemId}:
    delete:
      tags: [playback]
      operationId: removePlaybackQueueItem
      summary: Remove item from queue
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/queueItemId"
        - name: If-Match
          in: header
          required: true
          description: Queue Revision on which removal intent is based
          schema:
            type: string
      responses:
        "200":
          description: Updated queue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Queue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Queue revision conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueConflictResponse"
  /api/v1/playback/queue/events:
    get:
      tags: [playback]
      operationId: streamPlaybackQueueEvents
      summary: Stream Queue invalidation events
      description: >-
        Emits queue-invalidated server-sent events. Event IDs carry the persisted
        event sequence, while data carries that sequence and the latest Queue
        Revision so reconnecting clients can detect stale state and refetch the
        Queue.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Queue event stream
          content:
            text/event-stream:
              schema:
                $ref: "#/components/schemas/QueueEvent"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/tracks/{trackId}/stream:
    get:
      tags: [playback]
      operationId: streamTrack
      summary: Stream track audio with HTTP Range support
      parameters:
        - $ref: "#/components/parameters/trackId"
      responses:
        "200":
          description: Full audio stream
          content:
            audio/*:
              schema:
                type: string
                format: binary
        "206":
          description: Partial content (Range request)
          content:
            audio/*:
              schema:
                type: string
                format: binary
        "404":
          $ref: "#/components/responses/NotFound"
        "416":
          $ref: "#/components/responses/RangeNotSatisfiable"
  /api/v1/playlists:
    get:
      tags: [playlists]
      operationId: listPlaylists
      summary: List playlists
      responses:
        "200":
          description: Playlist list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlaylistList"
    post:
      tags: [playlists]
      operationId: createPlaylist
      summary: Create playlist
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlaylistCreate"
      responses:
        "201":
          description: Created playlist
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Playlist"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/playlists/{playlistId}:
    get:
      tags: [playlists]
      operationId: getPlaylist
      summary: Get playlist with tracks
      parameters:
        - $ref: "#/components/parameters/playlistId"
      responses:
        "200":
          description: Playlist detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlaylistDetail"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/playlists/{playlistId}/tracks:
    post:
      tags: [playlists]
      operationId: addPlaylistTrack
      summary: Add track to playlist
      parameters:
        - $ref: "#/components/parameters/playlistId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlaylistTrackAdd"
      responses:
        "200":
          description: Updated playlist
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlaylistDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/playlists/{playlistId}/tracks/{trackId}:
    delete:
      tags: [playlists]
      operationId: removePlaylistTrack
      summary: Remove track from playlist
      parameters:
        - $ref: "#/components/parameters/playlistId"
        - $ref: "#/components/parameters/trackId"
      responses:
        "200":
          description: Updated playlist
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlaylistDetail"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/radio/stations:
    get:
      tags: [radio]
      operationId: listRadioStations
      summary: List user radio stations
      responses:
        "200":
          description: Radio station list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RadioStationList"
    post:
      tags: [radio]
      operationId: createRadioStation
      summary: Create user radio station
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RadioStationCreate"
      responses:
        "201":
          description: Created radio station
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RadioStation"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/radio/stations/{stationId}:
    get:
      tags: [radio]
      operationId: getRadioStation
      summary: Get user radio station
      parameters:
        - $ref: "#/components/parameters/stationId"
      responses:
        "200":
          description: Radio station detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RadioStation"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [radio]
      operationId: patchRadioStation
      summary: Update user radio station
      parameters:
        - $ref: "#/components/parameters/stationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RadioStationPatch"
      responses:
        "200":
          description: Updated radio station
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RadioStation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [radio]
      operationId: deleteRadioStation
      summary: Delete user radio station
      parameters:
        - $ref: "#/components/parameters/stationId"
      responses:
        "204":
          description: Radio station deleted
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/radio/search:
    get:
      tags: [radio]
      operationId: searchRadioStations
      summary: Search Radio Browser stations
      parameters:
        - name: q
          in: query
          schema:
            type: string
        - name: country
          in: query
          schema:
            type: string
        - name: language
          in: query
          schema:
            type: string
        - name: tag
          in: query
          schema:
            type: string
        - name: codec
          in: query
          schema:
            type: string
        - name: codecGroup
          in: query
          schema:
            type: string
            enum: [aac]
        - name: minBitrate
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
      responses:
        "200":
          description: Radio search results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RadioSearchResultList"
  /api/v1/radio/catalog/countries:
    get:
      tags: [radio]
      operationId: listRadioCatalogCountries
      summary: List Radio Browser catalog countries
      responses:
        "200":
          description: Radio Browser country options
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RadioCatalogOptionList"
  /api/v1/radio/catalog/tags:
    get:
      tags: [radio]
      operationId: listRadioCatalogTags
      summary: List Radio Browser catalog tags
      responses:
        "200":
          description: Radio Browser tag options
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RadioCatalogOptionList"
  /api/v1/radio/import:
    post:
      tags: [radio]
      operationId: importRadioStation
      summary: Import Radio Browser station
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RadioImportRequest"
      responses:
        "201":
          description: Imported radio station
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RadioStation"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/radio/preview:
    post:
      tags: [radio]
      operationId: previewRadioStation
      summary: Stream a Radio Browser search result before importing it
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RadioImportRequest"
      responses:
        "200":
          description: Radio preview stream
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
            audio/*:
              schema:
                type: string
                format: binary
            application/vnd.apple.mpegurl:
              schema:
                type: string
                format: binary
            application/x-mpegurl:
              schema:
                type: string
                format: binary
            video/*:
              schema:
                type: string
                format: binary
            application/octet-stream:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequest"
        "415":
          description: DRM-protected HLS is unsupported
        "502":
          description: Upstream radio resource failed
        "503":
          description: Radio streaming is unavailable on this Music Server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/radio/preview/{stationUuid}/stream:
    get:
      tags: [radio]
      operationId: streamRadioPreview
      summary: Stream Radio Browser station preview
      parameters:
        - name: stationUuid
          in: path
          required: true
          schema:
            type: string
        - name: resource
          in: query
          description: Signed HLS resource token emitted by a proxied playlist
          schema:
            type: string
      responses:
        "200":
          description: Radio preview stream
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
            audio/*:
              schema:
                type: string
                format: binary
            application/vnd.apple.mpegurl:
              schema:
                type: string
                format: binary
            application/x-mpegurl:
              schema:
                type: string
                format: binary
            video/*:
              schema:
                type: string
                format: binary
            application/octet-stream:
              schema:
                type: string
                format: binary
        "206":
          description: Partial HLS media resource
          content:
            audio/*:
              schema:
                type: string
                format: binary
            video/*:
              schema:
                type: string
                format: binary
            application/octet-stream:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequest"
        "415":
          description: DRM-protected HLS is unsupported
        "502":
          description: Upstream radio resource failed
  /api/v1/radio/stations/{stationId}/stream:
    get:
      tags: [radio]
      operationId: streamRadioStation
      summary: Stream radio station through backend proxy
      parameters:
        - $ref: "#/components/parameters/stationId"
        - name: resource
          in: query
          description: Signed HLS resource token emitted by a proxied playlist
          schema:
            type: string
      responses:
        "200":
          description: Radio stream
          content:
            audio/*:
              schema:
                type: string
                format: binary
            application/vnd.apple.mpegurl:
              schema:
                type: string
                format: binary
            application/x-mpegurl:
              schema:
                type: string
                format: binary
            video/*:
              schema:
                type: string
                format: binary
            application/octet-stream:
              schema:
                type: string
                format: binary
        "206":
          description: Partial HLS media resource
          content:
            audio/*:
              schema:
                type: string
                format: binary
            video/*:
              schema:
                type: string
                format: binary
            application/octet-stream:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "415":
          description: DRM-protected HLS is unsupported
        "502":
          description: Upstream radio resource failed
  /api/v1/radio/stations/{stationId}/now-playing:
    get:
      tags: [radio]
      operationId: getRadioNowPlaying
      summary: Get latest radio now-playing metadata
      parameters:
        - $ref: "#/components/parameters/stationId"
      responses:
        "200":
          description: Now-playing metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RadioNowPlaying"
        "404":
          $ref: "#/components/responses/NotFound"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  responses:
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: Conflict
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InsufficientStorage:
      description: Managed Storage cannot satisfy the configured reserve and temporary-space requirements
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    RangeNotSatisfiable:
      description: Range not satisfiable
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  parameters:
    limit:
      name: limit
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 500
        default: 50
    offset:
      name: offset
      in: query
      schema:
        type: integer
        minimum: 0
        default: 0
    albumId:
      name: albumId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    trackId:
      name: trackId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    importId:
      name: importId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    batchId:
      name: batchId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    queueItemId:
      name: itemId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    playlistId:
      name: playlistId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    stationId:
      name: stationId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  schemas:
    HealthResponse:
      type: object
      required: [status, version, capabilities]
      properties:
        status:
          type: string
          enum: [ok]
        version:
          type: string
        capabilities:
          type: array
          items:
            type: string
          description: >-
            Named server behaviors supported by this release. The versioned
            /api/v1 surface itself is advertised as api.v1. Queue event streaming
            is advertised as playback.queue-events.v1 and the first strict FLAC
            Managed Import tracer bullet as managed-import.v1. Multi-file Managed
            Import Batches are advertised as managed-import-batches.v1, Permanent
            Track Deletion as managed-track-deletion.v1, and explicit Track
            Replacement as managed-track-replacement.v1. Clients gate optional
            behavior on the exact capability name and must ignore unknown
            entries so newer servers stay compatible with older clients.
    User:
      type: object
      required: [id, username]
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        displayName:
          type: string
    LayoutPreferences:
      type: object
      required: [sidebarPosition, panels, collapsed]
      properties:
        sidebarPosition:
          type: string
          enum: [left, right]
        panels:
          type: object
          required: [left, right]
          properties:
            left:
              type: array
              items:
                type: string
            right:
              type: array
              items:
                type: string
        collapsed:
          type: object
          required: [left, right]
          properties:
            left:
              type: boolean
            right:
              type: boolean
        sizes:
          type: array
          items:
            type: number
          minItems: 3
          maxItems: 3
          description: Panel widths as percentages [left, main, right]
    ThemePreferences:
      type: object
      required: [mode, preset]
      properties:
        mode:
          type: string
          enum: [light, dark, system]
        preset:
          type: string
          enum:
            - earthly
            - tokyo-night
            - vintage-harbor
            - night-ember
            - dusty-earth
            - coastal-mist
            - sage-hearth
    UserPreferences:
      type: object
      required: [theme, layout]
      properties:
        theme:
          $ref: "#/components/schemas/ThemePreferences"
        layout:
          $ref: "#/components/schemas/LayoutPreferences"
    UserPreferencesPatch:
      type: object
      properties:
        theme:
          $ref: "#/components/schemas/ThemePreferences"
        layout:
          $ref: "#/components/schemas/LayoutPreferences"
    ErrorResponse:
      type: object
      required: [error, code, message]
      properties:
        error:
          type: string
        code:
          type: string
        message:
          type: string
        field:
          type: string
          description: Machine-readable field associated with a structured validation failure.
        reason:
          type: string
          description: Actionable reason for a structured validation failure.
    ManagedImportJob:
      type: object
      required: [id, status, revision, validationProgress]
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          enum: [uploading, awaiting_confirmation, committed, failed]
        revision:
          type: integer
          minimum: 1
        validationProgress:
          type: integer
          minimum: 0
          maximum: 100
        errorCode:
          type: string
          description: Stable failure code when status is failed.
        trackId:
          type: string
          format: uuid
          description: Created or replaced Track when status is committed.
        replacesTrackId:
          type: string
          format: uuid
          description: Present when this job is an explicit Track Replacement of that Track.
    ManagedImportJobCreate:
      type: object
      required: [batchId, clientFileId]
      properties:
        batchId:
          type: string
          format: uuid
          description: Import Batch that owns this per-file job.
        clientFileId:
          type: string
          format: uuid
          description: Client-generated identifier used to correlate a Batch file when the create response is lost.
    ManagedImportBatch:
      type: object
      required: [id, status, revision, files]
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          enum: [uploading, confirming, completed]
        revision:
          type: integer
          minimum: 1
        files:
          type: array
          items:
            $ref: "#/components/schemas/ManagedImportBatchFile"
    ManagedImportBatchFile:
      type: object
      required: [jobId, state, status, revision, validationProgress, selected]
      properties:
        jobId:
          type: string
          format: uuid
        clientFileId:
          type: string
          format: uuid
        state:
          type: string
          enum: [accepted, rejected, unresolved, completed]
        status:
          type: string
          enum: [uploading, awaiting_confirmation, committed, failed]
        revision:
          type: integer
          minimum: 1
        validationProgress:
          type: integer
          minimum: 0
          maximum: 100
        originalFilename:
          type: string
        selected:
          type: boolean
        preview:
          $ref: "#/components/schemas/ManagedImportPreview"
        errorCode:
          type: string
        errorField:
          type: string
        errorReason:
          type: string
        outcome:
          type: string
          enum: [imported, rejected, failed, replaced, not_attempted]
        trackId:
          type: string
          format: uuid
    ManagedImportHistoryList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          maxItems: 20
          items:
            $ref: "#/components/schemas/ManagedImportHistoryItem"
    ManagedImportHistoryItem:
      type: object
      required: [importId, startedAt, completedAt, resultCode, counts, files]
      properties:
        importId:
          type: string
          format: uuid
        startedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
        resultCode:
          type: string
          enum: [completed, partially_completed, failed, canceled]
        counts:
          $ref: "#/components/schemas/ManagedImportHistoryCounts"
        files:
          type: array
          items:
            $ref: "#/components/schemas/ManagedImportHistoryFile"
    ManagedImportHistoryCounts:
      type: object
      required: [total, imported, rejected, failed, replaced, notAttempted, canceled]
      properties:
        total:
          type: integer
          minimum: 0
        imported:
          type: integer
          minimum: 0
        rejected:
          type: integer
          minimum: 0
        failed:
          type: integer
          minimum: 0
        replaced:
          type: integer
          minimum: 0
        notAttempted:
          type: integer
          minimum: 0
        canceled:
          type: integer
          minimum: 0
    ManagedImportHistoryFile:
      type: object
      required: [fileId, jobId, startedAt, completedAt, resultCode]
      properties:
        fileId:
          type: string
          format: uuid
        jobId:
          type: string
          format: uuid
        safeFilename:
          type: string
          maxLength: 255
        startedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
        contentSha256:
          type: string
          pattern: "^[0-9a-f]{64}$"
        resultCode:
          type: string
          minLength: 1
        createdTrackId:
          type: string
          format: uuid
        replacedTrackId:
          type: string
          format: uuid
    ManagedImportBatchConfirmation:
      type: object
      required: [revision, selectedFileIds]
      properties:
        revision:
          type: integer
          minimum: 1
        selectedFileIds:
          type: array
          uniqueItems: true
          items:
            type: string
            format: uuid
        duplicateDecisions:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/ManagedImportDuplicateDecision"
    ManagedImportDuplicateDecision:
      type: object
      required: [jobId, action]
      properties:
        jobId:
          type: string
          format: uuid
        action:
          type: string
          enum: [import_separately, replace_existing, do_not_import]
    ManagedImportPreview:
      type: object
      required: [jobId, status, revision, file, duplicateClassification]
      properties:
        jobId:
          type: string
          format: uuid
        status:
          type: string
          enum: [awaiting_confirmation, failed]
        revision:
          type: integer
          minimum: 1
        file:
          $ref: "#/components/schemas/ManagedImportPreviewFile"
        duplicateClassification:
          type: string
          enum: [none, exact_duplicate, possible_duplicate]
        duplicateCandidates:
          type: array
          items:
            $ref: "#/components/schemas/ManagedImportDuplicateCandidate"
        replacement:
          $ref: "#/components/schemas/TrackReplacementPreview"
    ManagedImportDuplicateCandidate:
      type: object
      required: [trackId, title, artists, album, discNo, trackNo, format, durationMs]
      properties:
        trackId:
          type: string
          format: uuid
        title:
          type: string
        artists:
          type: array
          minItems: 1
          items:
            type: string
        album:
          type: string
        discNo:
          type: integer
          minimum: 1
        trackNo:
          type: integer
          minimum: 1
        format:
          type: string
        durationMs:
          type: integer
          minimum: 1
    ManagedImportPreviewFile:
      oneOf:
        - $ref: "#/components/schemas/ManagedImportFlacPreviewFile"
        - $ref: "#/components/schemas/ManagedImportWavPreviewFile"
        - $ref: "#/components/schemas/ManagedImportM4aPreviewFile"
        - $ref: "#/components/schemas/ManagedImportMp3PreviewFile"
        - $ref: "#/components/schemas/ManagedImportOggPreviewFile"
        - $ref: "#/components/schemas/ManagedImportOpusPreviewFile"
      discriminator:
        propertyName: format
        mapping:
          flac: "#/components/schemas/ManagedImportFlacPreviewFile"
          wav: "#/components/schemas/ManagedImportWavPreviewFile"
          m4a: "#/components/schemas/ManagedImportM4aPreviewFile"
          mp3: "#/components/schemas/ManagedImportMp3PreviewFile"
          ogg: "#/components/schemas/ManagedImportOggPreviewFile"
          opus: "#/components/schemas/ManagedImportOpusPreviewFile"
    ManagedImportPreviewFileCommon:
      type: object
      required:
        - originalFilename
        - title
        - artists
        - albumArtists
        - album
        - genres
        - trackNo
        - discNo
        - durationMs
        - sampleRateHz
        - channelCount
        - bitrateKbps
        - artworkMediaType
      properties:
        originalFilename:
          type: string
        title:
          type: string
        artists:
          type: array
          minItems: 1
          items:
            type: string
        albumArtists:
          type: array
          minItems: 1
          items:
            type: string
        album:
          type: string
        genres:
          type: array
          minItems: 1
          items:
            type: string
        trackNo:
          type: integer
          minimum: 1
        trackTotal:
          type: integer
          minimum: 1
        discNo:
          type: integer
          minimum: 1
        discTotal:
          type: integer
          minimum: 1
        year:
          type: integer
        durationMs:
          type: integer
          minimum: 1
        sampleRateHz:
          type: integer
          minimum: 1
        channelCount:
          type: integer
          minimum: 1
        bitrateKbps:
          type: integer
          minimum: 1
        artworkMediaType:
          type: string
          enum: [image/jpeg, image/png, image/webp]
    ManagedImportFlacPreviewFile:
      allOf:
        - $ref: "#/components/schemas/ManagedImportPreviewFileCommon"
        - type: object
          required: [format, container, codec, bitDepth]
          properties:
            format:
              type: string
              enum: [flac]
            container:
              type: string
              enum: [flac]
            codec:
              type: string
              enum: [flac]
            bitDepth:
              type: integer
              minimum: 1
    ManagedImportWavPreviewFile:
      allOf:
        - $ref: "#/components/schemas/ManagedImportPreviewFileCommon"
        - type: object
          required: [format, container, codec, bitDepth]
          properties:
            format:
              type: string
              enum: [wav]
            container:
              type: string
              enum: [wav]
            codec:
              type: string
              enum: ["pcm_u8", "pcm_s16le", "pcm_s24le", "pcm_s32le"]
            bitDepth:
              type: integer
              enum: [8, 16, 24, 32]
    ManagedImportM4aPreviewFile:
      allOf:
        - $ref: "#/components/schemas/ManagedImportPreviewFileCommon"
        - type: object
          required: [format, container, codec]
          properties:
            format:
              type: string
              enum: [m4a]
            container:
              type: string
              enum: [m4a]
            codec:
              type: string
              enum: [aac, alac]
            bitDepth:
              type: integer
              minimum: 1
              description: >-
                Source bit depth, present only for lossless ALAC. Lossy AAC
                previews omit the field because no source bit depth exists.
    ManagedImportMp3PreviewFile:
      allOf:
        - $ref: "#/components/schemas/ManagedImportPreviewFileCommon"
        - type: object
          required: [format, container, codec]
          properties:
            format:
              type: string
              enum: [mp3]
            container:
              type: string
              enum: [mp3]
            codec:
              type: string
              enum: [mp3]
    ManagedImportOggPreviewFile:
      allOf:
        - $ref: "#/components/schemas/ManagedImportPreviewFileCommon"
        - type: object
          required: [format, container, codec]
          properties:
            format:
              type: string
              enum: [ogg]
            container:
              type: string
              enum: [ogg]
            codec:
              type: string
              enum: [vorbis]
    ManagedImportOpusPreviewFile:
      allOf:
        - $ref: "#/components/schemas/ManagedImportPreviewFileCommon"
        - type: object
          required: [format, container, codec]
          properties:
            format:
              type: string
              enum: [opus]
            container:
              type: string
              enum: [ogg]
            codec:
              type: string
              enum: [opus]
    ManagedImportConfirmation:
      type: object
      required: [revision]
      properties:
        revision:
          type: integer
          minimum: 1
        duplicateDecision:
          type: string
          enum: [import_separately, replace_existing, do_not_import]
    ManagedImportResult:
      type: object
      required: [jobId, status, revision, trackId]
      properties:
        jobId:
          type: string
          format: uuid
        status:
          type: string
          enum: [committed]
        revision:
          type: integer
          minimum: 1
        trackId:
          type: string
          format: uuid
    Artist:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
        name:
          type: string
        albumCount:
          type: integer
    ArtistCredit:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
        name:
          type: string
    Genre:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
        name:
          type: string
    ReleaseIdentifier:
      type: object
      required: [scheme, value]
      properties:
        scheme:
          type: string
        value:
          type: string
    AlbumArtwork:
      type: object
      required: [contentSha256, sizeBytes]
      properties:
        sourceTrackId:
          type: string
          format: uuid
        contentSha256:
          type: string
          pattern: "^[0-9a-f]{64}$"
        mediaType:
          type: string
          enum: [image/jpeg, image/png, image/webp]
        width:
          type: integer
          minimum: 1
        height:
          type: integer
          minimum: 1
        sizeBytes:
          type: integer
          format: int64
          minimum: 1
    ArtistList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Artist"
        total:
          type: integer
    Album:
      type: object
      required: [id, title, artistId, artistName]
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        artistId:
          type: string
        artistName:
          type: string
        albumArtists:
          type: array
          description: Ordered normalized Album Artist credits.
          items:
            $ref: "#/components/schemas/ArtistCredit"
        year:
          type: integer
        releaseDate:
          type: string
          description: Known release date or year for this Album edition.
        releaseIdentifiers:
          type: array
          description: External identifiers that disambiguate this Album edition.
          items:
            $ref: "#/components/schemas/ReleaseIdentifier"
        trackCount:
          type: integer
        genres:
          type: array
          deprecated: true
          description: Legacy Genre names retained for API v1 compatibility.
          items:
            type: string
        genreItems:
          type: array
          description: Normalized Genres derived from active Tracks.
          items:
            $ref: "#/components/schemas/Genre"
        artwork:
          $ref: "#/components/schemas/AlbumArtwork"
    AlbumList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Album"
        total:
          type: integer
    AlbumDetail:
      allOf:
        - $ref: "#/components/schemas/Album"
        - type: object
          required: [tracks]
          properties:
            tracks:
              type: array
              items:
                $ref: "#/components/schemas/Track"
    Track:
      type: object
      required: [id, title, artistName, albumId, durationMs, format]
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        artistName:
          type: string
          deprecated: true
          description: Legacy display credit retained for API v1 compatibility.
        artists:
          type: array
          description: Ordered normalized Track Artist credits.
          items:
            $ref: "#/components/schemas/ArtistCredit"
        albumId:
          type: string
          format: uuid
        albumTitle:
          type: string
        trackNo:
          type: integer
        discNo:
          type: integer
          minimum: 1
          description: Effective disc position; single-disc Albums use 1.
        trackTotal:
          type: integer
          minimum: 1
        discTotal:
          type: integer
          minimum: 1
        durationMs:
          type: integer
        format:
          type: string
        codec:
          type: string
        container:
          type: string
        sampleFormat:
          type: string
        genre:
          type: string
          deprecated: true
          description: Legacy Genre value retained for API v1 compatibility.
        genres:
          type: array
          description: Ordered normalized Track Genres.
          items:
            $ref: "#/components/schemas/Genre"
        sampleRateHz:
          type: integer
        bitDepth:
          type: integer
        channelCount:
          type: integer
        bitrateBps:
          type: integer
        bitrateKbps:
          type: integer
        sizeBytes:
          type: integer
        replayGain:
          $ref: "#/components/schemas/ReplayGainMetadata"
    ReplayGainMetadata:
      type: object
      required: [trackGainDb, trackPeak, albumGainDb, albumPeak]
      properties:
        trackGainDb:
          type: number
          format: double
          nullable: true
        trackPeak:
          type: number
          format: double
          nullable: true
        albumGainDb:
          type: number
          format: double
          nullable: true
        albumPeak:
          type: number
          format: double
          nullable: true
    TrackList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Track"
        total:
          type: integer
    Playlist:
      type: object
      required: [id, name, isDefault, trackCount]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        isDefault:
          type: boolean
        trackCount:
          type: integer
    PlaylistList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Playlist"
        total:
          type: integer
    PlaylistDetail:
      allOf:
        - $ref: "#/components/schemas/Playlist"
        - type: object
          required: [tracks]
          properties:
            tracks:
              type: array
              items:
                $ref: "#/components/schemas/Track"
    PlaylistCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
    PlaylistTrackAdd:
      type: object
      required: [trackId]
      properties:
        trackId:
          type: string
          format: uuid
    RadioNowPlaying:
      type: object
      properties:
        title:
          type: string
        artist:
          type: string
        raw:
          type: string
        updatedAt:
          type: string
          format: date-time
        stale:
          type: boolean
    RadioStation:
      type: object
      required: [id, name, streamUrl, tags, source, isFavorite, position]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        streamUrl:
          type: string
          format: uri
        homepageUrl:
          type: string
          format: uri
        faviconUrl:
          type: string
          format: uri
        country:
          type: string
        language:
          type: string
        tags:
          type: array
          items:
            type: string
        codec:
          type: string
        bitrate:
          type: integer
        source:
          type: string
        externalId:
          type: string
        isFavorite:
          type: boolean
        position:
          type: integer
        lastNowPlaying:
          $ref: "#/components/schemas/RadioNowPlaying"
    RadioStationList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RadioStation"
        total:
          type: integer
    RadioStationCreate:
      type: object
      required: [name, streamUrl]
      properties:
        name:
          type: string
        streamUrl:
          type: string
          format: uri
        homepageUrl:
          type: string
          format: uri
        faviconUrl:
          type: string
          format: uri
        country:
          type: string
        language:
          type: string
        tags:
          type: array
          items:
            type: string
        codec:
          type: string
        bitrate:
          type: integer
        source:
          type: string
        externalId:
          type: string
        isFavorite:
          type: boolean
    RadioStationPatch:
      type: object
      properties:
        name:
          type: string
        streamUrl:
          type: string
          format: uri
        homepageUrl:
          type: string
          format: uri
        faviconUrl:
          type: string
          format: uri
        country:
          type: string
        language:
          type: string
        tags:
          type: array
          items:
            type: string
        codec:
          type: string
        bitrate:
          type: integer
        isFavorite:
          type: boolean
        position:
          type: integer
    RadioSearchResult:
      type: object
      required: [stationUuid, name, streamUrl, tags]
      properties:
        stationUuid:
          type: string
        name:
          type: string
        streamUrl:
          type: string
          format: uri
        homepageUrl:
          type: string
          format: uri
        faviconUrl:
          type: string
          format: uri
        country:
          type: string
        language:
          type: string
        tags:
          type: array
          items:
            type: string
        codec:
          type: string
        bitrate:
          type: integer
        votes:
          type: integer
        healthStatus:
          type: string
          enum: [healthy, broken, unknown]
        lastCheckedAt:
          type: string
          format: date-time
        lastSuccessfulAt:
          type: string
          format: date-time
    RadioSearchResultList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RadioSearchResult"
        total:
          type: integer
    RadioCatalogOption:
      type: object
      required: [name]
      properties:
        name:
          type: string
        code:
          type: string
        stationCount:
          type: integer
    RadioCatalogOptionList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RadioCatalogOption"
        total:
          type: integer
    RadioImportRequest:
      type: object
      properties:
        stationUuid:
          type: string
        result:
          $ref: "#/components/schemas/RadioSearchResult"
    DeleteResult:
      type: object
      required: [deletedFiles]
      properties:
        deletedFiles:
          type: integer
    AlbumDeletionPreview:
      type: object
      required: [albumId, albumTitle, trackCount, totalSizeBytes, tracks, playlistReferences, queueReferences, confirmationToken]
      properties:
        albumId:
          type: string
          format: uuid
        albumTitle:
          type: string
        trackCount:
          type: integer
          minimum: 1
        totalSizeBytes:
          type: integer
          minimum: 0
        tracks:
          type: array
          items:
            $ref: "#/components/schemas/AlbumDeletionTrack"
        playlistReferences:
          type: array
          items:
            $ref: "#/components/schemas/TrackDeletionPlaylistReference"
        queueReferences:
          type: array
          items:
            $ref: "#/components/schemas/TrackDeletionQueueReference"
        confirmationToken:
          type: string
          description: Opaque token binding confirmation to the reviewed Album, its Tracks, managed files, and relationships
    AlbumDeletionTrack:
      type: object
      required: [trackId, trackTitle, sizeBytes]
      properties:
        trackId:
          type: string
          format: uuid
        trackTitle:
          type: string
        sizeBytes:
          type: integer
          minimum: 0
    AlbumDeletionFailed:
      type: object
      required: [trackId, trackTitle, reason]
      properties:
        trackId:
          type: string
          format: uuid
        trackTitle:
          type: string
        reason:
          type: string
    AlbumDeletionResult:
      type: object
      required: [deleted, failed, deletedFiles]
      properties:
        deleted:
          type: array
          items:
            $ref: "#/components/schemas/AlbumDeletionTrack"
        failed:
          type: array
          description: Empty when every Track was deleted; otherwise the Track that stopped the run
          items:
            $ref: "#/components/schemas/AlbumDeletionFailed"
        deletedFiles:
          type: integer
          minimum: 0
    TrackDeletionPreview:
      type: object
      required: [trackId, trackTitle, managedFile, playlistReferences, queueReferences, confirmationToken]
      properties:
        trackId:
          type: string
          format: uuid
        trackTitle:
          type: string
        managedFile:
          $ref: "#/components/schemas/TrackDeletionManagedFile"
        playlistReferences:
          type: array
          items:
            $ref: "#/components/schemas/TrackDeletionPlaylistReference"
        queueReferences:
          type: array
          items:
            $ref: "#/components/schemas/TrackDeletionQueueReference"
        confirmationToken:
          type: string
          description: Opaque token binding confirmation to the reviewed Track, managed file, and relationships
    TrackDeletionManagedFile:
      type: object
      required: [path, sizeBytes]
      properties:
        path:
          type: string
          description: Managed Storage-relative canonical audio path
        sizeBytes:
          type: integer
          minimum: 0
    TrackDeletionPlaylistReference:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
    TrackDeletionQueueReference:
      type: object
      required: [userId, itemCount]
      properties:
        userId:
          type: string
        itemCount:
          type: integer
          minimum: 1
    TrackDeletionConfirmation:
      type: object
      required: [confirmationToken]
      properties:
        confirmationToken:
          type: string
    TrackReplacementPreview:
      type: object
      description: Every visible consequence of replacing a Managed Track's audio file while keeping its identity.
      required:
        - trackId
        - trackTitle
        - sourceFormat
        - technicalProperties
        - metadata
        - library
        - artwork
        - canonicalPath
        - oldFile
        - playlistReferences
        - queueReferences
        - possibleDuplicates
        - confirmationToken
      properties:
        trackId:
          type: string
          format: uuid
        trackTitle:
          type: string
        sourceFormat:
          $ref: "#/components/schemas/TrackReplacementFieldDiff"
        technicalProperties:
          type: array
          items:
            $ref: "#/components/schemas/TrackReplacementFieldDiff"
        metadata:
          type: array
          items:
            $ref: "#/components/schemas/TrackReplacementFieldDiff"
        library:
          $ref: "#/components/schemas/TrackReplacementLibraryChange"
        artwork:
          $ref: "#/components/schemas/TrackReplacementArtworkChange"
        canonicalPath:
          $ref: "#/components/schemas/TrackReplacementFieldDiff"
        oldFile:
          $ref: "#/components/schemas/TrackReplacementFileDeletion"
        playlistReferences:
          type: array
          description: Playlist references that stay attached to the unchanged Track ID.
          items:
            $ref: "#/components/schemas/TrackDeletionPlaylistReference"
        queueReferences:
          type: array
          description: Queue references that stay attached to the unchanged Track ID.
          items:
            $ref: "#/components/schemas/TrackDeletionQueueReference"
        possibleDuplicates:
          type: array
          description: Other Tracks whose metadata resembles the replacement; informational only.
          items:
            $ref: "#/components/schemas/ManagedImportDuplicateCandidate"
        confirmationToken:
          type: string
          description: Opaque token binding confirmation to the reviewed Track, managed file, references, and replacement bytes
    TrackReplacementFieldDiff:
      type: object
      required: [field, current, replacement, isChanged]
      properties:
        field:
          type: string
        current:
          type: string
        replacement:
          type: string
        isChanged:
          type: boolean
    TrackReplacementLibraryChange:
      type: object
      required: [currentAlbumId, movesAlbum, createsAlbum, removesEmptyAlbum, removesEmptyArtists, createsArtists, createsGenres]
      properties:
        currentAlbumId:
          type: string
          format: uuid
        replacementAlbumId:
          type: string
          format: uuid
        movesAlbum:
          type: boolean
        createsAlbum:
          type: boolean
        removesEmptyAlbum:
          type: boolean
        removesEmptyArtists:
          type: array
          items:
            type: string
        createsArtists:
          type: array
          items:
            type: string
        createsGenres:
          type: array
          items:
            type: string
    TrackReplacementArtworkChange:
      type: object
      required: [currentMediaType, currentSha256, replacementMediaType, replacementSha256, isChanged, replacesAlbumArtwork]
      properties:
        currentMediaType:
          type: string
        currentSha256:
          type: string
        replacementMediaType:
          type: string
        replacementSha256:
          type: string
        isChanged:
          type: boolean
        replacesAlbumArtwork:
          type: boolean
          description: True when the Album's display artwork file itself is replaced.
    TrackReplacementFileDeletion:
      type: object
      required: [path, sizeBytes]
      properties:
        path:
          type: string
          description: Managed Storage-relative path of the previous managed file that will be deleted
        sizeBytes:
          type: integer
          minimum: 0
    TrackReplacementConfirmation:
      type: object
      required: [revision, confirmationToken]
      properties:
        revision:
          type: integer
          minimum: 1
        confirmationToken:
          type: string
    TrackReplacementResult:
      type: object
      required: [jobId, status, revision, trackId, deletedFiles]
      properties:
        jobId:
          type: string
          format: uuid
        status:
          type: string
          enum: [committed]
        revision:
          type: integer
          minimum: 1
        trackId:
          type: string
          format: uuid
        deletedFiles:
          type: integer
          minimum: 0
          format: date-time
    QueueItem:
      type: object
      required: [id, trackId, position, track]
      properties:
        id:
          type: string
          format: uuid
        trackId:
          type: string
          format: uuid
        position:
          type: integer
        track:
          $ref: "#/components/schemas/Track"
    Queue:
      type: object
      required: [items, revision]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/QueueItem"
        revision:
          type: string
          description: Opaque version of this Queue state
    QueueEvent:
      type: object
      required: [revision, sequence, invalidates]
      properties:
        revision:
          type: string
          description: Latest Queue Revision when this event was emitted
        sequence:
          type: string
          pattern: "^[0-9]+$"
          description: Monotonically increasing event order, independent from opaque Queue Revision semantics
        invalidates:
          type: array
          description: Shared resources clients must refetch
          items:
            type: string
            enum: [queue]
    QueueReplace:
      type: object
      required: [trackIds, revision]
      properties:
        trackIds:
          type: array
          items:
            type: string
            format: uuid
        revision:
          type: string
    QueueItemAppend:
      type: object
      required: [trackId, revision]
      properties:
        trackId:
          type: string
          format: uuid
        revision:
          type: string
    QueueReorder:
      type: object
      required: [itemIds, revision]
      properties:
        itemIds:
          type: array
          items:
            type: string
            format: uuid
        revision:
          type: string
    QueueConflictResponse:
      allOf:
        - $ref: "#/components/schemas/ErrorResponse"
        - type: object
          required: [queue]
          properties:
            queue:
              $ref: "#/components/schemas/Queue"
