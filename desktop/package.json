{
  "name": "@repo/desktop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "bash ../scripts/run-with-storage.sh desktop bash ../scripts/run-desktop.sh build",
    "build:sidecar": "pnpm build:sidecar:base",
    "build:sidecar:base": "bash ../scripts/run-with-storage.sh desktop bash ../scripts/run-desktop.sh sidecar",
    "build:sidecar:prepared": "bash ../scripts/run-with-storage.sh desktop bash ../scripts/run-desktop.sh prepared",
    "desktop:dev": "bash ../scripts/run-with-storage.sh desktop bash ../scripts/run-desktop.sh dev",
    "format": "cargo fmt --manifest-path src-tauri/Cargo.toml",
    "format:check": "cargo fmt --manifest-path src-tauri/Cargo.toml -- --check",
    "lint": "bash ../scripts/run-with-storage.sh desktop cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings",
    "prepare:mpv": "bash ../scripts/run-with-storage.sh desktop ./scripts/prepare-mpv-sidecar.sh",
    "start": "bash ../scripts/run-with-storage.sh desktop bash ../scripts/run-desktop.sh start",
    "test": "pnpm test:unit",
    "test:unit": "bash ../scripts/run-with-storage.sh test cargo test --manifest-path src-tauri/Cargo.toml",
    "test:mpv": "bash ../scripts/run-with-storage.sh test cargo test --manifest-path src-tauri/Cargo.toml --lib playback::tests -- --ignored --test-threads=1 --skip playback::tests::real_pinned_mpv_returns_from_exclusive_to_system_output",
    "test:mpv:hardware": "bash ../scripts/run-with-storage.sh test cargo test --manifest-path src-tauri/Cargo.toml playback::tests::real_pinned_mpv_returns_from_exclusive_to_system_output -- --ignored --test-threads=1",
    "test:import-parity": "EARTHLY_AUDIO_MEDIA_PROXY_PORT=0 bash ../scripts/run-with-storage.sh test cargo test --manifest-path src-tauri/Cargo.toml --test managed_import_parity -- --ignored --test-threads=1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "web": "workspace:*"
  }
}
